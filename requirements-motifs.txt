# Track B — EndMT spatial-motif pipeline (motifs/). Install on top of a
# PyTorch build that matches your CUDA version (see requirements.txt).
#
# torch_geometric may need the matching wheel index for scatter/sparse ops:
#   https://pytorch-geometric.readthedocs.io/en/latest/install/installation.html

torch>=2.1.0
torch_geometric>=2.4.0
numpy
pandas
pyarrow                 # embeddings.parquet / motifs.parquet
scipy
scikit-learn
scikit-image            # Stage 1 feature extraction
tifffile                # Stage 1 + motif-map figures
matplotlib
umap-learn              # Stage 3 QC + Stage 4 reduction
leidenalg               # Stage 4 Leiden clustering (k-means fallback if absent)
python-igraph
tqdm

# Stage 1 segmentation only (GPU recommended)
# cellpose>=4.0           # Cellpose-SAM
# numpy<2.0               # cellpose pin at time of writing

# Optional
# wandb                   # --wandb on train_encoder.py
