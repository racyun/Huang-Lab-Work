# Tested together: torch 2.5.x + timm 1.0.26 (CPU/GPU). Adjust torch for your CUDA wheel.
torch>=2.1.0,<2.7.0
torchvision>=0.16.0,<0.22.0
timm>=1.0.12,<2.0.0
PyYAML>=6.0.1
Pillow>=10.0.0
numpy>=1.24.0,<3.0.0
transformers>=4.36.0,<5.0.0
accelerate>=0.25.0
# Optional: pycocotools for COCO metrics
# pycocotools>=2.0.7

# Experiment tracking
wandb>=0.16.0

# Detection metrics
torchmetrics[detection]>=1.0.0   # includes pycocotools dependency
faster-coco-eval>=1.5.0          # optional: ~10x faster mAP computation on CPU
scipy>=1.10.0                    # required by Deformable-DETR Hungarian matcher

# Testing
pytest>=7.0
